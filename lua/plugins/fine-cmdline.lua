return {
    -- https://github.com/vonheikemen/fine-cmdline.nvim
    -- I think this plugin has a lot of potential. however, it doesn't have a lot of features yet.
    -- I'm going to keep an eye on it. so, commented it out for now.
    -- "VonHeikemen/fine-cmdline.nvim",
    -- requires = {
    -- 	{ "MunifTanjim/nui.nvim" },
    -- },
    -- config = function()
    -- 	vim.api.nvim_set_keymap("n", "<CR>", "<cmd>FineCmdline<CR>", { noremap = true })
    -- end,
}
